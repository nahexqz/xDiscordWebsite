# 📘 คู่มือการติดตั้ง Legendary Community
### ระบบ Discord Community Platform ฉบับสมบูรณ์

---

## 📋 สารบัญ

1. [ความต้องการของระบบ](#1-ความต้องการของระบบ)
2. [การเตรียมบัญชีและ API Keys](#2-การเตรียมบัญชีและ-api-keys)
3. [การติดตั้งโปรแกรมพื้นฐาน](#3-การติดตั้งโปรแกรมพื้นฐาน)
4. [การ Clone และติดตั้ง Project](#4-การ-clone-และติดตั้ง-project)
5. [การตั้งค่า Environment Variables](#5-การตั้งค่า-environment-variables)
6. [การตั้งค่าฐานข้อมูล](#6-การตั้งค่าฐานข้อมูล)
7. [การตั้งค่า Discord Application](#7-การตั้งค่า-discord-application)
8. [การตั้งค่า Stripe](#8-การตั้งค่า-stripe)
9. [การตั้งค่า Firebase](#9-การตั้งค่า-firebase)
10. [การตั้งค่า Cloudinary](#10-การตั้งค่า-cloudinary)
11. [การรัน Development Server](#11-การรัน-development-server)
12. [การ Deploy ขึ้น Production](#12-การ-deploy-ขึ้น-production)
13. [การตั้งค่า Admin Dashboard](#13-การตั้งค่า-admin-dashboard)
14. [การทดสอบระบบ](#14-การทดสอบระบบ)
15. [การแก้ปัญหาเบื้องต้น](#15-การแก้ปัญหาเบื้องต้น)

---

## 1. ความต้องการของระบบ

### ซอฟต์แวร์ที่ต้องติดตั้ง

| โปรแกรม | เวอร์ชัน | ดาวน์โหลด |
|---------|---------|-----------|
| Node.js | 20+ | https://nodejs.org |
| PostgreSQL | 15+ | https://postgresql.org |
| Git | ล่าสุด | https://git-scm.com |
| Docker (ถ้าใช้) | ล่าสุด | https://docker.com |

### บัญชีที่ต้องสมัคร

| บริการ | ใช้สำหรับ | ราคา |
|--------|---------|------|
| Discord Developer | OAuth2 + Bot | ฟรี |
| Stripe | รับชำระเงิน | ฟรี (คิดต่อเมื่อมีธุรกรรม) |
| Firebase | SMS OTP | ฟรี (มี quota) |
| Cloudinary | อัปโหลดรูป | ฟรี 25GB |
| Vercel (ถ้า deploy) | Hosting | ฟรีสำหรับ hobby |
| Railway (ถ้า deploy) | Bot + DB | ฟรี $5/เดือน |

---

## 2. การเตรียมบัญชีและ API Keys

### 2.1 Discord Developer Portal

1. ไปที่ https://discord.com/developers/applications
2. คลิก **"New Application"**
3. ตั้งชื่อแอป เช่น "Legendary Community"
4. คลิก **"Create"**

**รับ Client ID และ Client Secret:**
- ไปแท็บ **"OAuth2"** → คัดลอก **"CLIENT ID"**
- คลิก **"Reset Secret"** → คัดลอก **"CLIENT SECRET"**
- ในช่อง **"Redirects"** เพิ่ม:
  - `http://localhost:3000/api/auth/discord/callback` (สำหรับ dev)
  - `https://yourdomain.com/api/auth/discord/callback` (สำหรับ production)

**สร้าง Bot:**
- ไปแท็บ **"Bot"**
- คลิก **"Add Bot"** → ยืนยัน
- คัดลอก **"TOKEN"** (กด Reset Token ถ้าไม่เห็น)
- เปิด **Privileged Gateway Intents** ทั้ง 3 อัน:
  - ✅ Presence Intent
  - ✅ Server Members Intent  
  - ✅ Message Content Intent

**เชิญ Bot เข้า Server:**
- ไปแท็บ **"OAuth2"** → **"URL Generator"**
- เลือก Scope: `bot`, `applications.commands`
- เลือก Bot Permissions: `Administrator`
- คัดลอก URL → เปิดในเบราว์เซอร์ → เลือก Server → Authorize

**รับ Server ID และ Channel IDs:**
- เปิด Discord → Settings → Advanced → เปิด **"Developer Mode"**
- คลิกขวาที่ Server → **"Copy Server ID"** = `DISCORD_GUILD_ID`
- คลิกขวาที่ channel ที่ต้องการ → **"Copy Channel ID"**

**รับ Role IDs:**
- Server Settings → Roles → คลิกขวาที่ Role → Copy ID

---

### 2.2 Stripe

1. สมัครที่ https://stripe.com
2. ไป Dashboard → **"Developers"** → **"API keys"**
3. คัดลอก:
   - **Publishable key** (`pk_test_...`) = `STRIPE_PUBLISHABLE_KEY`
   - **Secret key** (`sk_test_...`) = `STRIPE_SECRET_KEY`

**ตั้งค่า Webhook:**
1. ไป **"Developers"** → **"Webhooks"** → **"Add endpoint"**
2. URL: `https://yourdomain.com/api/stripe/webhook`
3. Events ที่ต้องเลือก:
   - `checkout.session.completed`
   - `checkout.session.expired`
   - `payment_intent.payment_failed`
4. คัดลอก **"Signing secret"** = `STRIPE_WEBHOOK_SECRET`

> ⚠️ สำหรับ Development ใช้ Stripe CLI แทน:
> ```bash
> stripe login
> stripe listen --forward-to localhost:3000/api/stripe/webhook
> # คัดลอก whsec_... ที่ขึ้นมา
> ```

---

### 2.3 Firebase

1. ไป https://console.firebase.google.com
2. คลิก **"Add project"** → ตั้งชื่อ → Create
3. ในโปรเจค ไป **"Build"** → **"Authentication"**
4. คลิก **"Get started"**
5. แท็บ **"Sign-in method"** → เลือก **"Phone"** → Enable → Save

**รับ Config:**
1. ไป Project Settings (ไอคอนฟัน) → **"Your apps"**
2. คลิกไอคอน Web (`</>`)
3. ลงทะเบียนแอป (ใส่ชื่อ) → Copy config

```javascript
// config ที่ได้ จะมีลักษณะนี้:
const firebaseConfig = {
  apiKey: "AIza...",           // NEXT_PUBLIC_FIREBASE_API_KEY
  authDomain: "xxx.firebaseapp.com", // NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN
  projectId: "xxx",           // NEXT_PUBLIC_FIREBASE_PROJECT_ID
  storageBucket: "xxx.appspot.com", // NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET
  messagingSenderId: "123...", // NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID
  appId: "1:123:web:abc..."   // NEXT_PUBLIC_FIREBASE_APP_ID
};
```

**เพิ่ม Authorized Domains:**
- Authentication → Settings → Authorized domains
- เพิ่ม: `localhost`, `yourdomain.com`

---

### 2.4 Cloudinary

1. สมัครที่ https://cloudinary.com
2. จาก Dashboard คัดลอก:
   - **Cloud name** = `CLOUDINARY_CLOUD_NAME`
   - **API Key** = `CLOUDINARY_API_KEY`
   - **API Secret** = `CLOUDINARY_API_SECRET`

---

## 3. การติดตั้งโปรแกรมพื้นฐาน

### Windows

```powershell
# ติดตั้ง Node.js
# ดาวน์โหลดจาก https://nodejs.org เลือก LTS

# ติดตั้ง PostgreSQL
# ดาวน์โหลดจาก https://postgresql.org

# ติดตั้ง Git
# ดาวน์โหลดจาก https://git-scm.com

# ตรวจสอบการติดตั้ง
node --version   # ต้องได้ v20.x.x
npm --version    # ต้องได้ 10.x.x
psql --version   # ต้องได้ 15.x.x
```

### macOS

```bash
# ติดตั้ง Homebrew (ถ้ายังไม่มี)
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"

# ติดตั้ง Node.js
brew install node@20

# ติดตั้ง PostgreSQL
brew install postgresql@15
brew services start postgresql@15

# ตรวจสอบ
node --version
psql --version
```

### Linux (Ubuntu/Debian)

```bash
# Node.js
curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
sudo apt-get install -y nodejs

# PostgreSQL
sudo apt install postgresql-15 postgresql-client-15
sudo systemctl start postgresql
sudo systemctl enable postgresql

# ตรวจสอบ
node --version
psql --version
```

---

## 4. การ Clone และติดตั้ง Project

```bash
# แตก zip ไฟล์ที่ดาวน์โหลด หรือ clone จาก Git
unzip legendary-community.zip
cd legendary-community

# หรือถ้าใช้ Git:
# git clone https://github.com/yourorg/legendary-community.git
# cd legendary-community

# ติดตั้ง dependency ของ Web App
cd apps/web
npm install

# ติดตั้ง dependency ของ Discord Bot
cd ../bot
npm install

# กลับมา root
cd ../..
```

> ⏱️ การติดตั้งอาจใช้เวลา 2-5 นาที ขึ้นอยู่กับความเร็วอินเทอร์เน็ต

---

## 5. การตั้งค่า Environment Variables

### สำหรับ Web App

```bash
# คัดลอก template
cp .env.example apps/web/.env.local

# เปิดไฟล์แก้ไข
# Windows: notepad apps/web/.env.local
# Mac/Linux: nano apps/web/.env.local
```

แก้ไขค่าต่างๆ ในไฟล์ `apps/web/.env.local`:

```env
# ==========================================
# APP SETTINGS
# ==========================================
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_APP_NAME="Legendary Community"
NODE_ENV=development

# ==========================================
# DATABASE
# ==========================================
# รูปแบบ: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL="postgresql://postgres:YOUR_PASSWORD@localhost:5432/legendary_community"

# ==========================================
# JWT & SESSION (สร้าง random string อย่างน้อย 32 ตัวอักษร)
# วิธีสร้าง: เปิด terminal พิมพ์ openssl rand -hex 32
# ==========================================
JWT_SECRET=your_random_secret_key_minimum_32_characters_here
SESSION_SECRET=another_random_secret_key_here

# ==========================================
# DISCORD (จาก Discord Developer Portal)
# ==========================================
DISCORD_CLIENT_ID=1234567890123456789
DISCORD_CLIENT_SECRET=abcdefghijklmnop
DISCORD_REDIRECT_URI=http://localhost:3000/api/auth/discord/callback
NEXT_PUBLIC_DISCORD_CLIENT_ID=1234567890123456789
DISCORD_BOT_TOKEN=your_bot_token_here
DISCORD_GUILD_ID=your_server_id
DISCORD_ADMIN_CHANNEL_ID=channel_id_for_admin_notifications
DISCORD_LOG_CHANNEL_ID=channel_id_for_logs
DISCORD_TICKET_CHANNEL_ID=channel_id_for_tickets
DISCORD_INVITE_URL=https://discord.gg/your_invite_code

# Role IDs สำหรับ Verification (คลิกขวา Role → Copy ID)
DISCORD_ROLE_VERIFIED_L1=role_id_level_1
DISCORD_ROLE_VERIFIED_L2=role_id_level_2
DISCORD_ROLE_VERIFIED_L3=role_id_level_3

# ==========================================
# STRIPE
# ==========================================
STRIPE_SECRET_KEY=sk_test_your_secret_key
STRIPE_PUBLISHABLE_KEY=pk_test_your_publishable_key
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_your_publishable_key
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret
STRIPE_CURRENCY=thb

# ==========================================
# CLOUDINARY
# ==========================================
CLOUDINARY_CLOUD_NAME=your_cloud_name
CLOUDINARY_API_KEY=your_api_key
CLOUDINARY_API_SECRET=your_api_secret
NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME=your_cloud_name

# ==========================================
# FIREBASE (Phone OTP)
# ==========================================
NEXT_PUBLIC_FIREBASE_API_KEY=AIza...
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your-project-id
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your-project.appspot.com
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=123456789
NEXT_PUBLIC_FIREBASE_APP_ID=1:123:web:abc

# ==========================================
# ADMIN
# Discord ID ของ admin (ใครอยู่ในนี้จะเข้า admin dashboard ได้)
# ถ้ามีหลายคน คั่นด้วย comma: id1,id2,id3
# ==========================================
ADMIN_DISCORD_IDS=your_discord_user_id
```

### สำหรับ Discord Bot

```bash
cp .env.example apps/bot/.env
```

แก้ไข `apps/bot/.env`:

```env
DATABASE_URL="postgresql://postgres:YOUR_PASSWORD@localhost:5432/legendary_community"
DISCORD_BOT_TOKEN=your_bot_token_here
DISCORD_GUILD_ID=your_server_id
DISCORD_ADMIN_CHANNEL_ID=channel_id
DISCORD_LOG_CHANNEL_ID=channel_id
DISCORD_TICKET_CHANNEL_ID=channel_id
DISCORD_ROLE_VERIFIED_L1=role_id
DISCORD_ROLE_VERIFIED_L2=role_id
DISCORD_ROLE_VERIFIED_L3=role_id
```

### วิธีหา Discord ID ของตัวเอง

1. เปิด Discord → Settings → Advanced → เปิด Developer Mode
2. คลิกขวาที่ชื่อตัวเองใน server → **"Copy User ID"**
3. นำ ID ที่ได้ไปใส่ใน `ADMIN_DISCORD_IDS`

---

## 6. การตั้งค่าฐานข้อมูล

### 6.1 สร้าง PostgreSQL User และ Database

```bash
# เข้า PostgreSQL
psql -U postgres

# สร้าง database
CREATE DATABASE legendary_community;

# (ตัวเลือก) สร้าง user แยก
CREATE USER lc_user WITH PASSWORD 'your_strong_password';
GRANT ALL PRIVILEGES ON DATABASE legendary_community TO lc_user;

# ออก
\q
```

### 6.2 รัน Prisma Migration

```bash
cd apps/web

# Generate Prisma Client
npx prisma generate

# รัน Migration (สร้าง tables ทั้งหมด)
npx prisma migrate dev --name init

# ถ้า migrate ไม่ได้ ลองใช้ db push แทน
# npx prisma db push
```

### 6.3 Seed ข้อมูลตัวอย่าง

```bash
# รัน seed script (สร้าง categories, products, coupons ตัวอย่าง)
npx tsx prisma/seed.ts
```

ข้อมูลที่ถูก seed:
- 📁 3 หมวดหมู่สินค้า: VIP Roles, Special Roles, Event Roles
- 🎭 4 สินค้าตัวอย่าง: VIP Member, Legendary Member, Supporter Badge, Event Champion
- 🏷️ 1 coupon: `WELCOME20` (ลด 20%)
- 🤖 7 Bot message templates

### 6.4 ตรวจสอบ Database

```bash
# เปิด Prisma Studio (UI สำหรับดูข้อมูล)
npx prisma studio
# เปิดเบราว์เซอร์ที่ http://localhost:5555
```

---

## 7. การตั้งค่า Discord Application

### 7.1 ลำดับชั้นของ Role

⚠️ **สำคัญมาก**: Bot ต้องมี role อยู่เหนือ role ที่ต้องการ assign

ในหน้า Server Settings → Roles:
```
[Admin] ← ของคุณ
[Bot Role] ← ต้องอยู่สูงกว่า role ข้างล่างทั้งหมด
├── [Legendary] ← role ที่ bot assign
├── [VIP Member]
├── [Supporter]
├── [Verified Level 3]
├── [Verified Level 2]
└── [Verified Level 1]
```

ลากให้ "Legendary Community Bot" อยู่เหนือทุก role ที่ bot ต้องการ assign

### 7.2 Channel Permissions

สร้าง channels สำหรับ admin notification:

| Channel | วัตถุประสงค์ | Setting |
|---------|------------|---------|
| `#admin-verifications` | รับการยืนยันตัวตน | Private, admin only |
| `#admin-logs` | บันทึก logs | Private, admin only |
| `#support-tickets` | รับ tickets | Private, admin only |

### 7.3 ตรวจสอบ Bot Permissions

Bot ต้องมี permissions เหล่านี้:
- ✅ Manage Roles
- ✅ Send Messages
- ✅ Read Messages/View Channels
- ✅ Send Messages in Threads
- ✅ Embed Links
- ✅ Use Slash Commands

---

## 8. การตั้งค่า Stripe

### 8.1 Test Mode vs Live Mode

- **Test Mode**: ใช้ตอน development, ไม่มีการโอนเงินจริง
- **Live Mode**: ใช้ตอน production จริง

### 8.2 Test Cards

| หมายเลขบัตร | ผลลัพธ์ |
|------------|---------|
| `4242 4242 4242 4242` | ชำระสำเร็จ |
| `4000 0025 0000 3155` | ต้องการ 3D Secure |
| `4000 0000 0000 9995` | ชำระไม่สำเร็จ |

ใช้ expiry ใดก็ได้ในอนาคต และ CVC ใดก็ได้ (เช่น `123`)

### 8.3 PromptPay (สำหรับลูกค้าไทย)

Stripe รองรับ PromptPay สำหรับ THB โดยอัตโนมัติ ไม่ต้องตั้งค่าเพิ่ม

### 8.4 Stripe Webhook สำหรับ Development

```bash
# ติดตั้ง Stripe CLI
# Mac: brew install stripe/stripe-cli/stripe
# Windows: https://stripe.com/docs/stripe-cli#install-on-windows

# Login
stripe login

# เปิด webhook forwarding (เปิดทิ้งไว้ระหว่าง dev)
stripe listen --forward-to localhost:3000/api/stripe/webhook

# จะได้ output แบบนี้:
# Ready! Your webhook signing secret is whsec_xxxxx (^C to quit)
# คัดลอก whsec_xxxxx ไปใส่ใน STRIPE_WEBHOOK_SECRET
```

---

## 9. การตั้งค่า Firebase

### 9.1 รองรับ SMS OTP สำหรับไทย

Firebase Phone Auth รองรับเบอร์โทรศัพท์ไทย (+66)

### 9.2 Test Phone Numbers (Development)

เพื่อประหยัด SMS quota ระหว่าง dev:
1. Firebase Console → Authentication → Sign-in method → Phone
2. เลื่อนลงหา **"Phone numbers for testing"**
3. เพิ่ม test number: `+66 812345678` → verification code: `123456`

### 9.3 App Check (Production)

สำหรับ production เพิ่ม App Check:
1. Firebase Console → App Check → Get started
2. เลือก reCAPTCHA Enterprise
3. ลงทะเบียน site ของคุณ

---

## 10. การตั้งค่า Cloudinary

### 10.1 Upload Presets (ตัวเลือก)

สำหรับรักษาความปลอดภัย:
1. Cloudinary Dashboard → Settings → Upload → Upload presets
2. สร้าง preset ชื่อ `lc_verifications`
3. Mode: Unsigned
4. Folder: `legendary-community/verifications`

### 10.2 Folders ที่ระบบสร้างอัตโนมัติ

| Folder | ใช้สำหรับ |
|--------|---------|
| `legendary-community/verifications/screenshots` | รูป Discord Profile |
| `legendary-community/verifications/selfies` | รูป Selfie |
| `legendary-community/verifications/id-cards` | รูป บัตรประชาชน |
| `legendary-community/tickets` | รูป Ticket screenshots |

---

## 11. การรัน Development Server

### เปิด 3 Terminal พร้อมกัน:

**Terminal 1 — Web App:**
```bash
cd apps/web
npm run dev

# จะได้:
# ✓ Ready on http://localhost:3000
```

**Terminal 2 — Discord Bot:**
```bash
cd apps/bot
npm run dev

# จะได้:
# ✅ Legendary Community Bot ready as BotName#0000
```

**Terminal 3 — Stripe Webhooks:**
```bash
stripe listen --forward-to localhost:3000/api/stripe/webhook

# จะได้:
# Ready! Your webhook signing secret is whsec_...
```

### ทดสอบการทำงาน

1. เปิดเบราว์เซอร์ไปที่ http://localhost:3000
2. คลิก **"JOIN DISCORD"** หรือ **"Login with Discord"**
3. อนุญาต OAuth2 → redirect กลับมา logged in

---

## 12. การ Deploy ขึ้น Production

### ตัวเลือก A: Vercel + Railway (แนะนำ)

#### Deploy Database บน Railway

1. สมัคร https://railway.app
2. New Project → Add PostgreSQL
3. คัดลอก `DATABASE_URL` จาก Variables tab
4. รัน migration:
```bash
DATABASE_URL="railway_url_here" npx prisma migrate deploy
DATABASE_URL="railway_url_here" npx tsx prisma/seed.ts
```

#### Deploy Web App บน Vercel

```bash
# ติดตั้ง Vercel CLI
npm i -g vercel

# Login
vercel login

# Deploy จากโฟลเดอร์ apps/web
cd apps/web
vercel --prod
```

ตั้งค่า Environment Variables บน Vercel Dashboard:
- ไป Project → Settings → Environment Variables
- เพิ่มทุกค่าจาก `.env.local` (เปลี่ยน localhost เป็น domain จริง)
- อย่าลืมเปลี่ยน `DISCORD_REDIRECT_URI` เป็น `https://yourdomain.vercel.app/api/auth/discord/callback`

#### Deploy Bot บน Railway

1. ใน Railway project → New Service → GitHub Repo
2. เลือก root directory: `apps/bot`
3. เพิ่ม Environment Variables เดียวกับ web app
4. Railway จะ auto-deploy เมื่อ push code

### ตัวเลือก B: Docker Compose (VPS)

```bash
# บน VPS ของคุณ
git clone your-repo
cd legendary-community

# แก้ไข .env
cp .env.example .env
nano .env

# Build และ start
docker-compose up -d --build

# รัน migration
docker-compose exec web npx prisma migrate deploy
docker-compose exec web npx tsx prisma/seed.ts

# ตรวจสอบ logs
docker-compose logs -f
```

### การตั้งค่า Domain และ SSL

```bash
# ติดตั้ง Certbot
sudo apt install certbot python3-certbot-nginx

# รับ SSL Certificate
sudo certbot --nginx -d yourdomain.com -d www.yourdomain.com

# ต่ออายุอัตโนมัติ
sudo certbot renew --dry-run
```

---

## 13. การตั้งค่า Admin Dashboard

### 13.1 เข้าใช้งาน Admin Dashboard

1. Login ด้วย Discord account ที่ ID อยู่ใน `ADMIN_DISCORD_IDS`
2. ไปที่ `/dashboard/admin`
3. หากไม่เห็น dashboard ตรวจสอบว่า Discord ID ถูกต้อง

### 13.2 เพิ่มสินค้าใหม่

1. Admin Dashboard → **Products** → **"+ New Product"**
2. กรอกข้อมูล:
   - **Name**: ชื่อสินค้า เช่น "VIP Member"
   - **Discord Role ID**: ID ของ Role ใน Discord Server
   - **Role Name**: ชื่อ Role ที่จะแสดงให้ผู้ใช้เห็น
   - **Price (THB)**: ราคา เช่น `99` (= ฿99)
   - **Stock**: จำนวน (ว่างเปล่า = ไม่จำกัด)
   - **Duration**: จำนวนวัน (ว่างเปล่า = ถาวร)
   - **Benefits**: สิทธิประโยชน์ต่างๆ
3. คลิก **"Create Product"**

### 13.3 ตรวจสอบ Verification Requests

1. Admin Dashboard → **Verifications**
2. เลือกดูตาม Level (1, 2, 3) หรือทั้งหมด
3. คลิกที่ request เพื่อดูรายละเอียดและรูปภาพ
4. คลิก **"Approve"** หรือ **"Reject"** (ต้องระบุเหตุผลถ้า reject)

หลัง Approve → Bot จะ:
- Assign role ใน Discord อัตโนมัติ
- ส่ง DM แจ้งผู้ใช้อัตโนมัติ

### 13.4 จัดการ Tickets

1. Admin Dashboard → **Tickets**
2. กรองตาม Status: Pending, In Progress, Resolved, etc.
3. คลิก expand เพื่อดูรายละเอียด
4. เปลี่ยน Status หรือ Reply ผ่าน dashboard
5. การ reply จะส่ง Discord DM ให้ผู้ใช้อัตโนมัติ

### 13.5 ตั้งค่า Bot Messages

1. Admin Dashboard → **Discord Bot**
2. แก้ไข template สำหรับ DM ต่างๆ
3. ใช้ variables: `{username}`, `{role}`, `{amount}`, `{level}`, `{reason}`, `{ticket_id}`
4. คลิก **"Save All Settings"**

### 13.6 สร้าง Announcement

1. Admin Dashboard → **Announcements**
2. กรอก Title และ Message
3. เลือก Type: info, warning, success, error
4. คลิก **"Publish"**
5. Popup จะปรากฏบน Homepage ให้ผู้ใช้เห็น (ซ่อนได้ 24 ชม.)

---

## 14. การทดสอบระบบ

### 14.1 ทดสอบ Discord Login

```
1. ไปที่ http://localhost:3000
2. คลิก "JOIN DISCORD"
3. อนุญาต permissions
4. ควร redirect กลับพร้อม logged in
5. เห็น avatar และ username ที่ navbar
```

### 14.2 ทดสอบการซื้อสินค้า

```
1. ไปที่ /shop
2. คลิก "Buy Now" บนสินค้าใดก็ได้
3. ใช้ test card: 4242 4242 4242 4242
   Expiry: 12/34, CVC: 123, Zip: 12345
4. คลิก Pay
5. ควร redirect ไป /success
6. ตรวจสอบ Discord ควรได้รับ role และ DM
```

### 14.3 ทดสอบ Verification

```
1. ไปที่ /verification
2. กรอกข้อมูลทั้งหมด
3. ทดสอบ OTP ด้วย test number (ถ้าตั้งไว้ใน Firebase)
4. Submit form
5. ตรวจสอบ admin channel ใน Discord
6. คลิก ✅ Approve ใน Discord
7. ผู้ใช้ควรได้รับ role และ DM
```

### 14.4 ทดสอบ Ticket

```
1. ไปที่ /tickets
2. คลิก "New Ticket"
3. กรอกข้อมูลและ submit
4. ตรวจสอบ ticket channel ใน Discord
5. ไป Admin Dashboard → Tickets
6. เปลี่ยน status หรือ reply
7. ผู้ใช้ควรได้รับ DM แจ้งการอัปเดต
```

---

## 15. การแก้ปัญหาเบื้องต้น

### ❌ ปัญหา: Discord Login ไม่ทำงาน

```
สาเหตุ: DISCORD_REDIRECT_URI ไม่ตรงกับที่ตั้งใน Discord Portal
แก้ไข:
1. ตรวจสอบ DISCORD_REDIRECT_URI ใน .env.local
2. เปิด Discord Developer Portal → OAuth2 → Redirects
3. ตรวจสอบว่า URL ตรงกัน (รวมถึง http vs https)
```

### ❌ ปัญหา: Bot ไม่สามารถ assign role ได้

```
สาเหตุ: Bot role ไม่สูงพอในลำดับชั้น
แก้ไข:
1. ไป Server Settings → Roles
2. ลาก "Bot Role" ให้อยู่เหนือ roles ทั้งหมดที่ bot ต้องการ assign
3. ตรวจสอบว่า Bot มี permission "Manage Roles"
```

### ❌ ปัญหา: Stripe Webhook ไม่ทำงาน

```
สาเหตุ: STRIPE_WEBHOOK_SECRET ไม่ถูกต้อง
แก้ไข (Development):
1. รัน stripe listen ใหม่
2. คัดลอก whsec_... ที่ขึ้นมา
3. อัปเดต STRIPE_WEBHOOK_SECRET ใน .env.local
4. Restart server

แก้ไข (Production):
1. ตรวจสอบ Stripe Dashboard → Webhooks
2. คัดลอก Signing secret ใหม่
```

### ❌ ปัญหา: Firebase OTP ไม่ส่ง SMS

```
สาเหตุ: Domain ไม่ได้รับอนุญาตใน Firebase
แก้ไข:
1. Firebase Console → Authentication → Settings → Authorized domains
2. เพิ่ม localhost และ domain ของคุณ

สาเหตุ: Quota หมด
แก้ไข: ใช้ Test Phone Number ระหว่าง development
```

### ❌ ปัญหา: รูปภาพ upload ไม่ได้

```
สาเหตุ: Cloudinary credentials ไม่ถูกต้อง
แก้ไข:
1. ตรวจสอบ CLOUDINARY_CLOUD_NAME, API_KEY, API_SECRET
2. ไป Cloudinary Dashboard → Settings → Access Keys
3. Regenerate key ถ้าจำเป็น
```

### ❌ ปัญหา: Database connection ล้มเหลว

```
แก้ไข:
1. ตรวจสอบ PostgreSQL รันอยู่:
   sudo systemctl status postgresql

2. ตรวจสอบ DATABASE_URL format:
   postgresql://USER:PASSWORD@HOST:PORT/DATABASE

3. ทดสอบ connection:
   psql "postgresql://postgres:password@localhost:5432/legendary_community"

4. สร้าง database ถ้ายังไม่มี:
   createdb legendary_community
```

### ❌ ปัญหา: Prisma generate error

```bash
# ลบ node_modules และติดตั้งใหม่
cd apps/web
rm -rf node_modules
npm install
npx prisma generate
```

---

## 📞 ขอความช่วยเหลือ

หากพบปัญหาที่ไม่ได้ระบุในคู่มือนี้:

1. **เปิด Ticket** บนเว็บไซต์ `/tickets`
2. **ตรวจสอบ Logs**:
   ```bash
   # Web app logs
   cd apps/web && npm run dev 2>&1 | tee logs.txt
   
   # Docker logs
   docker-compose logs -f web
   docker-compose logs -f bot
   ```
3. **Discord Community** — ติดต่อผ่าน Discord server

---

## 🔄 การอัปเดต Project

```bash
# Pull changes ล่าสุด
git pull origin main

# อัปเดต dependencies
cd apps/web && npm install
cd ../bot && npm install

# รัน migration ใหม่ (ถ้ามี)
cd ../web && npx prisma migrate deploy

# Restart services
# Docker:
docker-compose restart

# Vercel: deploy ใหม่
vercel --prod
```

---

*คู่มือนี้อัปเดตล่าสุด: มกราคม 2025*  
*เวอร์ชัน: Legendary Community v1.0.0*
